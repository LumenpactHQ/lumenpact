[package]
name = "commitment-escrow"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "27.0.0" }

[dev-dependencies]
soroban-sdk = { version = "27.0.0", features = ["testutils"] }
soroban-token-sdk = { version = "21.7.6" }